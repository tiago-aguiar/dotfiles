# Installing GccEmacs (on MacOS)

```console
$ brew install gcc
$ brew install libgccjit
$ brew install autoconf
$ brew install automake

$ git clone https://github.com/emacs-mirror/emacs.git ~/Applications/
$ cd ~/Applications/emacs
$ ./autogen.sh
$ ./configure --with-native-compilation
$ make -j$(nproc)
```
